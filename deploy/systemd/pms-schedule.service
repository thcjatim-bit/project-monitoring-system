[Unit]
Description=Project Monitoring System scheduler
After=network.target postgresql.service

[Service]
Type=oneshot
User=pms
Group=pms
WorkingDirectory=/opt/pms/current
ExecStart=/usr/bin/php artisan schedule:run --no-interaction
